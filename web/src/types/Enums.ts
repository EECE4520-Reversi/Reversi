export enum TileState {
  EMPTY,
  WHITE,
  BLACK,
  VIABLE,
}

export enum Difficulty {
  EASY,
  MEDIUM,
  HARD,
}

export enum GameType {
  LOCAL = 1,
  AI,
  ONLINE,
}

export enum GameState {
  PLAYER1 = 1,
  PLAYER2,
  GAMEOVER,
}
